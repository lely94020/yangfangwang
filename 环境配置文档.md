# 药房网商城 — 开发环境配置文档

## 1. 项目简介

药房网商城（yangfangwang）是一个基于 Java EE 的 B2C 药品电商平台，采用 Servlet + JSP 经典架构，提供前台商品浏览、购物车、下单支付以及后台商品、订单、用户、权限管理等完整功能。

---

## 2. 开发环境及版本要求

| 组件 | 版本 | 说明 |
|------|------|------|
| **JDK** | 11 (LTS) | Java 语言与运行时环境 |
| **Maven** | 3.6+ | 项目构建与依赖管理 |
| **Tomcat** | 9.x | Servlet 4.0 兼容的 Web 容器 |
| **MySQL** | 8.0+ | 关系型数据库 |
| **IntelliJ IDEA** | 2020+ (推荐 2023+) | 集成开发环境 |
| **操作系统** | macOS / Windows / Linux | 不限 |

---

## 3. 技术栈明细

| 技术 | 版本 | 用途 |
|------|------|------|
| Java Servlet API | 4.0.1 | HTTP 请求处理 |
| JSP API | 2.3.3 | 动态页面模板 |
| JSTL | 1.2 | JSP 标准标签库 |
| MySQL Connector/J | 8.0.33 | JDBC 数据库驱动 |
| Maven WAR Plugin | 3.3.2 | WAR 包打包 |
| UTF-8 | — | 项目编码 |

---

## 4. 环境搭建步骤

### 4.1 安装 JDK 11

```bash
# macOS (Homebrew)
brew install openjdk@11

# 验证
java -version
# 输出示例: openjdk version "11.0.29" 2025-10-21 LTS
```

### 4.2 安装 Maven

```bash
# macOS (Homebrew)
brew install maven

# 验证
mvn -version
```

### 4.3 安装 MySQL 8.0

```bash
# macOS (Homebrew)
brew install mysql@8.0

# 启动
brew services start mysql@8.0

# 安全初始化
mysql_secure_installation

# 验证
mysql --version
# 输出示例: mysql  Ver 8.0.45 for macos15 on arm64
```

### 4.4 安装 Tomcat 9

```bash
# macOS (Homebrew)
brew install tomcat@9

# 启动
brew services start tomcat@9

# 验证访问: http://localhost:8080
```

或手动下载并解压至本地目录，在 IDEA 中配置 Tomcat Server。

### 4.5 初始化数据库

```sql
mysql -u root -p < src/main/webapp/WEB-INF/init.sql
```

该脚本会自动创建 `yangfangwang` 数据库并导入表结构与测试数据。

**默认数据库连接配置**（`DBUtil.java`）：

| 参数 | 值 |
|------|-----|
| JDBC URL | `jdbc:mysql://localhost:3306/yangfangwang?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false` |
| 用户名 | `root` |
| 密码 | `12345678` |

> 如需修改数据库密码，请编辑 `src/main/java/com/yangfangwang/util/DBUtil.java` 中的 `PASSWORD` 常量。

---

## 5. IDEA 项目配置

### 5.1 导入项目

1. File → Open → 选择项目根目录 `yangfangwang`
2. 选择 `pom.xml`，以 Maven Project 方式导入
3. IDEA 自动下载依赖

### 5.2 配置 Tomcat

1. Run → Edit Configurations → `+` → Tomcat Server → Local
2. Application Server: 选择 Tomcat 9 安装目录
3. Deployment → `+` → Artifact → `yangfangwang:war exploded`
4. Application context: `/yangfangwang`（或 `/`）
5. 配置完成后启动 Tomcat

### 5.3 配置 JDK

1. File → Project Structure → Project → SDK → 选择 `11`
2. File → Project Structure → Project → Language level → `11`

### 5.4 Maven 配置

| 配置项 | 值 |
|--------|-----|
| `maven.compiler.source` | 11 |
| `maven.compiler.target` | 11 |
| `project.build.sourceEncoding` | UTF-8 |

---

## 6. 项目构建

```bash
# 编译
mvn compile

# 打包 WAR
mvn clean package

# 跳过测试打包
mvn clean package -DskipTests

# 输出路径: target/yangfangwang.war
```

---

## 7. 快速启动验证

| 模块 | 访问地址 | 默认账号 |
|------|----------|---------|
| 前台首页 | `http://localhost:8080/yangfangwang/` | — |
| 用户注册 | `http://localhost:8080/yangfangwang/pages/register.jsp` | — |
| 后台登录 | `http://localhost:8080/yangfangwang/admin/login.jsp` | admin / admin123 |
| 后台管理 | `http://localhost:8080/yangfangwang/admin/index.jsp` | — |

---

## 8. 项目目录结构

```
yangfangwang/
├── pom.xml                                    # Maven 构建文件
├── src/
│   ├── main/
│   │   ├── java/com/yangfangwang/
│   │   │   ├── dao/                           # 数据访问层
│   │   │   │   ├── CartDao.java
│   │   │   │   ├── CategoryDao.java
│   │   │   │   ├── MemberDao.java
│   │   │   │   ├── OrderDao.java
│   │   │   │   ├── ProductDao.java
│   │   │   │   ├── RoleDao.java
│   │   │   │   ├── SystemSettingDao.java
│   │   │   │   └── UserDao.java
│   │   │   ├── filter/                        # 过滤器
│   │   │   │   ├── AuthFilter.java
│   │   │   │   └── EncodingFilter.java
│   │   │   ├── model/                         # 实体模型
│   │   │   │   ├── Cart.java
│   │   │   │   ├── Category.java
│   │   │   │   ├── Member.java
│   │   │   │   ├── Order.java
│   │   │   │   ├── OrderItem.java
│   │   │   │   ├── Product.java
│   │   │   │   ├── Role.java
│   │   │   │   ├── SystemSetting.java
│   │   │   │   └── User.java
│   │   │   ├── servlet/                       # Servlet 控制器
│   │   │   │   ├── admin/                     # 后台管理
│   │   │   │   └── front/                     # 前台
│   │   │   └── util/                          # 工具类
│   │   │       ├── DBUtil.java                # 数据库连接
│   │   │       └── PageUtil.java              # 分页工具
│   │   └── webapp/
│   │       ├── WEB-INF/
│   │       │   ├── web.xml                    # Web 部署描述符
│   │       │   └── init.sql                   # 数据库初始化脚本
│   │       ├── admin/                         # 后台 JSP 页面
│   │       ├── pages/                         # 前台 JSP 页面
│   │       ├── js/                            # JavaScript 文件
│   │       └── index.jsp                      # 根入口
│   └── test/                                  # 测试目录（当前为空）
└── target/                                    # 构建输出
```

---

## 9. 常见问题

**Q: Tomcat 启动后访问页面报 404？**
检查 Deployment 中的 Application context 是否与服务地址一致。

**Q: 数据库连接失败？**
确认 MySQL 服务已启动，DBUtil.java 中的用户名密码与本地 MySQL 一致。

**Q: Maven 依赖下载慢？**
在 `~/.m2/settings.xml` 中配置阿里云镜像：

```xml
<mirror>
  <id>aliyunmaven</id>
  <mirrorOf>central</mirrorOf>
  <name>阿里云公共仓库</name>
  <url>https://maven.aliyun.com/repository/public</url>
</mirror>
```

**Q: JSP 页面中文乱码？**
确认 EncodingFilter 已正确配置，且数据库连接 URL 包含 `characterEncoding=utf-8`。
